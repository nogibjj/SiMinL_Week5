```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
INSERT INTO gradstudentsDB VALUES (
            Computer Science, 
            STEM, 
            1500, 
            1200,);
```

```sql
SELECT * FROM gradstudentsDB;
```

```sql
UPDATE gradstudentsDB SET 
        major=Computer Science, 
        major_category=
        STEM,
        grad_total=1500, grad_employed=1200
        WHERE id=1;
```

```sql
DELETE FROM gradstudentsDB WHERE id=1;
```

```sql
SELECT * FROM gradstudentsDB WHERE Major='CONSTRUCTION SERVICES'
```

